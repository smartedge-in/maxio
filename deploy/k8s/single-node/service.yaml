apiVersion: v1
kind: Service
metadata:
  name: maxio
  namespace: maxio
  labels:
    app.kubernetes.io/name: maxio
spec:
  selector:
    app.kubernetes.io/name: maxio
  ports:
    - name: http
      port: 9000
      targetPort: http